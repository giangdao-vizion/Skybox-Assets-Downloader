{
  "name": "skybox-asset-downloader",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx -y serve . -p 3000",
    "build": "mkdir -p dist && cp index.html dist/",
    "preview": "npx -y serve dist -p 3000"
  },
  "dependencies": {},
  "devDependencies": {}
}
